@model TBTracker.Models.Drug

@{
    ViewBag.Title = "EditDrug";
}

<h2>EditDrug</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Drug</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DrugInfoId,"Drug Name")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DrugInfoId")
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StartDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StartDate)
            @Html.ValidationMessageFor(model => model.StartDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EndDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EndDate)
            @Html.ValidationMessageFor(model => model.EndDate)
        </div>


        <div class="editor-label">
            @Html.LabelFor(model => model.TimesPerWeek)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TimesPerWeek)
            @Html.ValidationMessageFor(model => model.TimesPerWeek)
        </div>

        @Html.Hidden("PatientId")
        @Html.Hidden("DrugId")
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Edit", new { id = Model.PatientId })
</div>
