@model MediviseMVC.Models.Alert

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Alert</legend>
    
    <div class="common-box">
        <div class="common-label">AlertDate:</div>
        <div class="common-field">
            @Html.DisplayFor(model => model.AlertDate)
        </div>
    </div>

    <div class="common-box">
        <div class="common-label">AlertTypeId:</div>
        <div class="common-field">
            @Html.DisplayFor(model => model.AlertTypeId)
        </div>
    </div>

    <div class="common-box">
        <div class="common-label">PatientId:</div>
        <div class="common-field">
            @Html.DisplayFor(model => model.PatientId)
        </div>
    </div>

</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
